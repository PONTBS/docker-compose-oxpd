# OXPd Docker Compose Scripts 
For Running an OXPd environment locally.
includes:
 - redis
 - roam-embedded
 - roam-embedded-proxy

Oher Reqs:
 - a PSIM instance set up with a printer, to point oxpd at.
